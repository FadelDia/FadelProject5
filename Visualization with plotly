#un diagramme en barres du nombre de tremblements de terre par pays
import seaborn as sns
import matplotlib.pyplot as plt
import plotly.express as px

# Set the aesthetic style of the plots
sns.set(style='whitegrid')

# Create a count plot for the 'country' column
plt.figure(figsize=(12, 8))
ax = sns.countplot(y='country', data=df_final, order=df_final['country'].value_counts().index, palette='viridis')
ax.set_title('Count of Earthquakes by Country', fontsize=16)
ax.set_xlabel('Count', fontsize=14)
ax.set_ylabel('Country', fontsize=14)
plt.show()
#Ce diagramme montre le nombre de tremblements de terre enregistrés dans chaque pays, permettant de voir rapidement quels pays sont les plus affectés.
